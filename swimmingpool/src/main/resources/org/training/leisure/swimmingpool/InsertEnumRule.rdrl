package org.training.leisure.swimmingpool;

import java.lang.Number;
import org.chtijbug.example.swimmingpool.SeasonType;

rule "InsertEnumRule"
	dialect "java"
	when
		InsertPeriod( eVal : enumValue != null )
	then
		Period p= new Period();
		p.setSeasonType(SeasonType.vlaueOf(eVal));
		insert(p);
end
